# Node / Package Managers
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# LOCKFILES: Commit your chosen lockfile (e.g., pnpm-lock.yaml).
# Ignore other package managers' lockfiles.
package-lock.json
yarn.lock
# To commit pnpm-lock.yaml, ensure it's NOT listed below.

# Create React App build output
build

# Firebase Emulators & Debug Logs
firebase-debug.log
firebase-debug.*.log
firestore-debug.log
ui-debug.log
*.log
.firebase/emulators/*
!.firebase/emulators/emulators.json

# Firebase CLI state
.firebase/hosting.*.cache

# Cloud Functions build output & specific ignores
functions/node_modules
functions/lib # Compiled TypeScript
functions/.env # For environment variables like NOTION_API_TOKEN
# If functions directory also uses pnpm, its pnpm-lock.yaml will be committed unless specified here.
# We will ignore npm's lockfile if it appears in functions:
functions/package-lock.json

# IDE / Editor specific files
.idea
.vscode # Except for recommended extensions
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# Misc
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Coverage directory
coverage

# Terraform (if used)
.terraform
terraform.tfstate
terraform.tfstate.backup
*.tfvars

# Project IDX / Google Cloud Code
.idx
